# Generate each value independently with:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
# Scope production values to the production deploy context only.
CONSULTATION_ENCRYPTION_KEY=
CONSULTATION_KEY_VERSION=v1
CSRF_SECRET=
CONSULTATION_RETENTION_DAYS=90
CONSULTATION_STORAGE_DIR=consultation-data
ALLOWED_ORIGINS=https://web-production-6ef9.up.railway.app
