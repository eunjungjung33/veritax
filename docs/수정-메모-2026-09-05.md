# 홈페이지 수정 메모 — 2026-09-05

## 사진 반영

- 폴더에 제공된 `정은정.jpg`를 원본 수정 없이 `public/images/jung-eun-jung.jpg`에 복사했습니다. 두 파일의 SHA-256이 같으며 크기는 864 × 1184픽셀, 511,688바이트입니다.
- 메인 페이지와 구성원 소개 페이지의 정은정 대표 공인회계사 빈 사진 자리 두 곳에 연결했습니다.
- 기존 사진 틀과 반응형 높이를 유지하고, 사진은 위쪽 중앙을 기준으로 틀을 채우게 배치했습니다. 화면 비율에 따라 사진 가장자리 일부가 잘릴 수 있습니다.
- 대체 텍스트는 `정은정 대표 공인회계사`입니다.
- 변경 파일: `src/components/Shared.tsx`, `src/pages.tsx`, `src/styles.css`, `public/images/jung-eun-jung.jpg`.

## 확인되어 수정한 기술적 결함

`server/railway.ts`의 정적 파일 응답 처리를 수정했습니다.

- 수정 전: 존재하지 않는 `/assets/*.js` 요청에도 홈페이지 HTML을 HTTP 200으로 응답하고, 해당 잘못된 응답에 1년 캐시(`immutable`)를 지정했습니다. 존재하지 않는 `/images/` 사진도 HTML 200으로 응답했습니다.
- 수정 후: `/assets/`와 `/images/` 아래 파일이 없으면 HTTP 404 및 `no-store`로 응답하여 잘못된 파일 응답의 장기 저장을 막습니다.
- 일반 페이지 경로의 SPA 처리는 유지했습니다.

사진 반영과 위 결함 이외의 디자인·문구·기능은 변경하지 않았습니다.

## 검증 결과

- 원격 `main`의 선행 변경 11개 커밋을 보존하며 통합했습니다. 통합 커밋 `0d1bf99`에서 `npm run check`: 테스트 파일 7개·테스트 35개 통과 및 프로덕션 빌드 성공.
- 빌드 서버 HTTP 검증 19건 통과: 누락된 JS·이미지의 GET/HEAD, SPA 경로 8개, 정상 JS/CSS와 JPEG 3개, CSRF의 외부 출처 차단(403)·동일 출처 허용(200).
- 개발 서버에서 메인·소개 페이지와 사진의 HTTP 200, 사진의 `image/jpeg` 응답 확인.
- `npm audit`: 전체 의존성에서 보고된 취약점 0건.
- 배포 전 통합 커밋 `0d1bf99`에서 Gitleaks 8.30.1로 전체 Git 이력 23개 커밋(`--all`)을 검사하여 비밀정보 발견 0건, 종료 코드 0을 확인했습니다. 검사 도구는 공식 릴리스의 SHA-256을 확인했고 보고서는 비밀값을 가리도록 설정했습니다. 앞선 작업 파일 66개 검사도 발견 0건이었습니다.
- CSRF, 입력 검증, 암호화 저장, 보관기간 정리, 워크플로, 정적 경로를 점검했으며 추가로 입증된 결함은 없었습니다. 알려진 비밀키 패턴 검색에서도 일치 항목이 없었습니다.
- 최종 코드 diff를 검토하여 요청 범위의 수정만 있는지 확인했고, `git diff --check`를 통과했습니다.

## 푸시·배포 및 운영 검증

- 코드 커밋 `0d1bf9935bf73a2c0b267ab25aa436736fe8edbc`를 `main`에 푸시했습니다. 원격 CI 실행 `33931161934`와 CodeQL 실행 `33931161985`는 모두 성공했습니다.
- 자동 배포가 시작되지 않아 기존 Railway 서비스에 소스 배포를 실행했습니다. 배포 `a62b86cc-372d-49a9-b7f4-9f83f470ee24`가 2026-09-05 08:56 KST에 `SUCCESS` 상태가 되었습니다.
- 운영 주소: <https://web-production-43f8e.up.railway.app>.
- 운영 홈·소개·상태 확인 경로 및 참조 JS/CSS·사진 모두 HTTP 200을 확인했습니다. 운영 JS에 새 사진 경로가 포함되었고, 운영 사진의 크기와 SHA-256이 사용자 원본과 일치합니다.
- 운영 보안 읽기 검증 12건 통과: 주요 페이지 보안 헤더(CSP, HSTS, `nosniff`, `DENY`, `no-referrer`), 누락 JS·이미지 GET/HEAD의 404·`no-store`, CSRF 동일 출처 허용·외부 출처 차단, 보안 쿠키 속성(`Secure`, `HttpOnly`, `SameSite=Strict`, `__Host-`, `Path=/`, Domain 미설정), 상담 API의 GET 차단, HTTP에서 HTTPS로의 전환을 확인했습니다. CSP에 `unsafe-inline`·`unsafe-eval` 허용은 없었습니다.
- `npm run tls:check` 통과: HTTPS 200, 검사 시 인증서 잔여 기간 52일.
- 위 운영 검증은 코드 커밋 `0d1bf99` 배포 기준입니다. 이 메모를 포함하는 후속 문서 커밋은 코드 변경 없이 별도로 반영합니다.

## 검증 한계 및 반영 상태

- 브라우저 도구가 `missing sandboxPolicy` 오류로 시작되지 않아 실제 화면의 데스크톱·모바일 시각 검증은 수행하지 못했습니다. 사진 원본 일치, 코드 배치 및 HTTP 응답을 확인했습니다.
- 루트의 사용자 제공 `정은정.jpg`와 작업지시서 DOCX는 커밋 대상에서 제외합니다. 실제 상담 제출은 수행하지 않았습니다.
- 점검한 코드·의존성·Git 이력·운영 HTTP 응답에서 추가로 확인된 보안 결함은 없습니다. 침투 테스트나 운영 저장소·접수 데이터의 직접 검사는 수행하지 않았습니다.

## 추가 작업 — 제공 로고 전체 적용

이번 작업은 앞선 배포 완료 커밋 `1780de5`를 기준으로 진행했습니다.

- 모든 페이지가 공유하는 헤더의 기존 `JEJ` 로고를 `로고 간단.png`로, 푸터의 기존 `JEJ` 로고를 `로고 full.png`로 교체했습니다. 모바일도 같은 공통 로고를 사용합니다.
- 원본을 수정하지 않고 각각 `public/images/jej-logo-simple.png`, `public/images/jej-logo-full.png`에 복사했습니다. 원본과 복사본의 SHA-256이 각각 일치합니다.
- 간단본은 1400 × 500픽셀·15,242바이트, 전체본은 1800 × 1200픽셀·40,228바이트입니다. PNG 디코딩과 CRC 검사에 통과했고 메타데이터 또는 파일 끝의 추가 데이터는 없었습니다.
- 원본 비율을 유지하고, 헤더 홈 링크와 상호 대체 텍스트를 유지했습니다. 기존 로고 교체로 쓰이지 않게 된 전용 CSS만 정리했습니다. 본문·메뉴·브랜드 설명 문구는 유지했습니다.
- 기존 로고 사용처를 전수 검색하여 헤더·푸터 두 곳의 교체를 확인했습니다. 기존 파비콘·웹앱 매니페스트·공유용 이미지 로고 설정은 없었습니다.
- 변경 파일은 `src/components/Layout.tsx`, `src/styles.css`, `src/design.css`, 위 PNG 두 개 및 이 메모입니다.
- 로고 적용 후 `npm run check`: 테스트 파일 7개·테스트 35개 통과 및 프로덕션 빌드 성공. 새 PNG 두 개의 HTTP 200·`image/png` 응답과 파일 해시 일치를 확인했습니다. 최종 diff 범위 및 `git diff --check`를 통과했습니다.
- 추가 보안 점검: `npm audit` 취약점 0건, Gitleaks 전체 이력 24개 커밋 및 현재 작업 파일 76개·빌드 JS/CSS 검사에서 비밀정보 발견 0건을 확인했습니다. 운영 민감 경로 8곳과 소스맵 요청에서 원문 노출이 없었고, 상담 저장 폴더가 공개 경로 밖에 있으며 Git에서 제외된 상태와 일반 사용자 허용 ACL이 없음을 확인했습니다.
- 로컬 HTTP 회귀 검증 17건 통과: 새 로고 응답·원본 바이트 일치, 정상 페이지 4개, 누락 JS/PNG GET·HEAD의 404·`no-store`, 민감 경로 4곳의 원문 미노출, CSRF 출처 검증, 상담 API GET 차단. 입증된 신규 취약점은 없어 이번 추가 작업에서는 보안 코드를 변경하지 않았습니다.
- 브라우저 도구는 `missing sandboxPolicy` 오류로 실행되지 않았으나 Edge 헤드리스 대체 렌더링으로 시각 검증을 완료했습니다. 1440픽셀 데스크톱 헤더, 500픽셀 모바일 헤더와 메뉴, 개인정보 처리방침 페이지의 푸터 전체 로고에서 잘림·왜곡·겹침이 없음을 확인했습니다.
- 로고 코드 커밋 `fb10cfb7ac54d07d64d06b5a9d309bf20c1af833`를 `main`에 푸시했습니다. CI 실행 `33934451403`과 CodeQL 실행 `33934451506`이 모두 성공했습니다.
- Railway 배포 `0fd554d6-ff15-4e00-ac6f-d4b489bff941`는 같은 코드 커밋으로 2026-09-05 09:55:12 KST에 `SUCCESS`가 되었습니다. 운영 홈·소개·개인정보 처리방침·상태 확인 경로 모두 HTTP 200이며, 운영 JS에 로고 경로 두 개가 포함된 것을 확인했습니다.
- 운영 로고 두 개의 HTTP 200·`image/png` 응답 및 원본과 동일한 바이트 크기·SHA-256을 확인했습니다. 누락 JS/PNG 요청의 404·`no-store` 동작도 유지됩니다. 위 운영 검증은 로고 코드 커밋 `fb10cfb` 배포 기준이며, 이 완료 기록은 코드 변경 없는 후속 문서 커밋으로 반영합니다.
- 운영 사이트도 Edge 실제 렌더링으로 확인했습니다. 500픽셀 모바일 헤더의 로고·메뉴 버튼, 1440픽셀 개인정보 처리방침 페이지의 헤더·푸터 로고에서 잘림·왜곡·겹침이 없었습니다.
- 루트의 사용자 제공 로고 두 개·정은정 사진·작업지시서 DOCX와 로컬 시각 검증 산출물은 커밋에서 제외했습니다. 실제 상담 제출이나 접수 데이터 열람은 수행하지 않았습니다.

## 추가 작업 — 로고 배경 투명화 및 어두운 배경 가독성

- 사용자 승인에 따라 Python·Pillow로 원본 로고의 단색 배경만 제거했습니다. 글자를 다시 그리지 않고 원색과 좌표·여백·캔버스 크기를 보존했으며, 글자 내부 빈 공간도 실제 알파 투명도로 처리했습니다.
- 새 파일은 `public/images/jej-logo-simple-transparent.png`(1400 × 500픽셀·11,735바이트)와 `public/images/jej-logo-full-transparent.png`(1800 × 1200픽셀·31,524바이트)입니다. 가장자리 픽셀은 원색과 투명도를 복원해 처리했고, 원래 배경색에 재합성한 결과 모든 RGB 픽셀이 원본과 정확히 일치했습니다.
- 헤더·푸터를 새 파일 경로에 연결하여 기존 이미지 캐시와 구분했습니다. 로고 크기와 배치는 유지하고, 투명한 글자·심볼 가장자리에만 0.25픽셀 회백색 윤곽과 약한 1픽셀 빛 효과를 적용했습니다. 사각형 배경은 추가하지 않았습니다.
- 코드 참조가 없어진 기존 배경 포함 PNG 두 개는 `public/images`에서 정리했습니다. 루트의 사용자 원본은 수정하지 않았습니다.
- 재현 스크립트는 `scripts/make-transparent-logos.py`입니다. 루트에 제공 원본 로고 두 개를 두고 Pillow가 설치된 Python으로 실행합니다. 이번 처리 환경은 Python 3.11.9·Pillow 12.2.0입니다.
- 이번 변경 범위는 로고 이미지·재현 스크립트·`src/components/Layout.tsx`의 이미지 경로 두 곳·`src/styles.css`의 로고 전용 효과와 이 메모입니다. 본문·메뉴·기타 레이아웃과 보안 코드는 변경하지 않았습니다.
- 로컬 Edge 시각 검증에서 정확히 390픽셀·1440픽셀 화면의 투명 배경, 상호 가독성, 기존 크기 유지, 메뉴 동작을 확인했습니다. 가로 넘침과 브라우저 예외는 없었으며 윤곽 강도를 한 차례 낮춰 과도한 빛 번짐을 줄였습니다.
- 최종 `npm run check`: 테스트 35개 및 프로덕션 빌드 통과. 빌드 서버 HTTP 검증 11건에서 페이지·보안 헤더, 새 PNG 두 개의 MIME·바이트 일치·실제 투명도, 제거된 구 PNG 및 누락 파일의 404·`no-store`, CSRF 외부 출처 차단과 상담 GET 차단을 확인했습니다.
- 이 추가 기록은 푸시 전 로컬 로고 검증이 완료된 시점 기준입니다. 최종 diff 검토와 `git diff --check`를 통과했으며, 실제 상담 제출 없이 확인했습니다. 푸시·배포 결과는 작업 완료 보고에서 별도로 전달합니다.
